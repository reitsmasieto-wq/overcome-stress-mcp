# =============================================================================
# Overcome Stress — L402 Skill Server Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   nano .env
# =============================================================================

# LNbits API keys (get these from LNbits admin panel after first setup)
# Step 1: Start with docker compose up -d
# Step 2: Open http://your-server-ip:5000
# Step 3: Create a wallet, copy the API keys
LNBITS_API_KEY=your-invoice-read-key-here
LNBITS_ADMIN_KEY=your-admin-key-here

# Server secret for signing macaroons (auto-generated if empty, but set one for persistence)
# Generate with: openssl rand -hex 32
SERVER_SECRET=your-random-secret-here

# Your domain (update Caddyfile too)
DOMAIN=skills.overcomestress.com
